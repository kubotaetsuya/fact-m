
<%= form_with model: @user, url: user_registration_path, class: 'registration-main', local: true do |f| %>
<div>
  <div>
    <h1>
      新規登録
    </h1>
  </div>

  
  <%# <%= render 'shared/error_messages', model: @user %> 
  

  <div>
    <div>
      <label>ニックネーム</label>
      <span>必須</span>
    </div>
    <%= f.text_area :nickname, class:"input-default", id:"nickname", placeholder:"ニックネームを入れてください", maxlength:"40" %>
  </div>
  <div>
    <div>
      <label>メールアドレス</label>
      <span>必須</span>
    </div>
    <%= f.email_field :email, class:"input-default", id:"email", autofocus: true %>
  </div>
  <div>
    <div>
      <label>パスワード</label>
      <span>必須</span>
    </div>
    <%= f.password_field :password, class:"input-default", id:"password", placeholder:"6文字以上の半角英数字" %>
    <p>※英字と数字の両方を含めて設定してください</p>
  </div>
  <div>
    <div>
      <label>パスワード(確認)</label>
      <span>必須</span>
    </div>
    <%= f.password_field :password_confirmation, class:"input-default", id:"password-confirmation", placeholder:"同じパスワードを入力して下さい" %>
  </div>
<div>

    <div>
      <label>ステータス</label>
      <span>必須</span>
    </div>
  <%= f.collection_select(:status_id, Status.all, :id, :name, {}, {class:"genre-select"}) %>
    <p>当てはまるものがない場合はその他で入力してください。</p>
  </div>

  <div>
    <div>
      <label>生年月日</label>
      <span>必須</span>
    </div>
    <div class='input-birth-wrap'>
      <%= raw sprintf(
                  f.date_select(
                    :birthday,
                    class:'select-birth',
                    id:"birth-date",
                    use_month_numbers: true,
                    prompt:'--',
                    start_year: 1930,
                    end_year: (Time.now.year - 5),
                    date_separator: '%s'),
                  "<p> 年 </p>", "<p> 月 </p>") + "<p> 日 </p>" %>
    </div>
  </div>
  <div class='register-btn'>
    <%= f.submit "新規登録" ,class:"register-red-btn" %>
  </div>
</div>
<% end %>